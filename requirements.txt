# Authoritative Python dependencies for PerhapsThisBugWillDo / Weevil-Lunar.
# Versions pinned with lower bounds to keep CI reproducible while allowing
# patch updates.
numpy>=1.26,<3
scipy>=1.11,<2
matplotlib>=3.8,<4
pyyaml>=6.0,<7
