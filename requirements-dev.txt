# Development and CI-only dependencies for PerhapsThisBugWillDo / Weevil-Lunar.
# Runtime dependencies live in requirements.txt; install both in CI:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0,<10      # tested: 9.0.3
ruff>=0.6,<1         # lint gate
mypy>=1.10,<2        # type-check gate
types-PyYAML>=6.0    # type stubs for the yaml import used by validators/tests
